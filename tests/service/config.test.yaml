server:
    address: "0.0.0.0"
    port: 7000

logging:
    folder: logs
    level: debug

scheduler:
    enable: true
    items:
        - name: balance
          enable: true
          expression: "*/1 * * * * *"

setting:
    agora_scan_url: "http://localhost:3600"
    agora_cl_node_url: "http://localhost:3333"
